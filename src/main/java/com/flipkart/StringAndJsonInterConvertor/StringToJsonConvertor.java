package com.flipkart.StringAndJsonInterConvertor;

//import org.json.simple.JSONArray;
//import org.json.simple.JSONObject;
//import org.json.simple.parser.JSONParser;

import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class StringToJsonConvertor {
    public static void main(String[] args) {
//        JSONParser parser = new JSONParser();
//        JSONObject json = (JSONObject) parser.parse({"phonetype":"N95","cat":"WP"});

       // JSONObject json = (JSONObject) JSONSerializer.toJSON(data);

//        JSONArray jsonArray = new JSONArray({"phonetype":"N95","cat":"WP"});
//
//        return IntStream.range(0, jsonArray.length())
//                .mapToObj(index -> ((JSONObject)jsonArray.get(index)).optString("cat"))
//                .collect(Collectors.toList());

        //final Object node = new ObjectMapper().readValue("{\"phonetype\":\"N95\",\"cat\":\"WP\"}", Object.class);

    }
}
